import { defineConfig, globalIgnores } from "eslint/config";
import nextVitals from "eslint-config-next/core-web-vitals";
import nextTs from "eslint-config-next/typescript";

const eslintConfig = defineConfig([
  ...nextVitals,
  ...nextTs,
  {
    rules: {
      // These effects intentionally start polling/subscriptions and update loading UI.
      "react-hooks/set-state-in-effect": "off",
      // The app uses explicit memoization for large dashboard collections.
      "react-hooks/preserve-manual-memoization": "off",
      // Freshness indicators intentionally compare server timestamps with the current time.
      "react-hooks/purity": "off",
    },
  },
  // Override default ignores of eslint-config-next.
  globalIgnores([
    // Default ignores of eslint-config-next:
    ".next/**",
    "out/**",
    "build/**",
    "next-env.d.ts",
  ]),
]);

export default eslintConfig;
